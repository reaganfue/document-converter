# 文件轉檔工具 — 專案開發企劃

> 最後更新：2026-06-11（迭代一輪完成）｜ 專案狀態：**功能完成，維護模式**

---

## 1. 專案定位

**一句話**：完全本機運行的個人文書工具箱 —— 文件格式互轉 + PDF 工具 + 圖片去背，零雲端依賴、零訂閱費用、零隱私外洩。

**要解決的問題**：線上轉檔服務有浮水印、次數限制、檔案需上傳第三方伺服器。對於日常文書處理需求，這些限制與隱私風險都不必要。

**目標用戶**：開發者本人（個人使用）。不以上架、分發、商業化為目標；曾有商用化評估（W-COMMERCIAL），現階段結論為「個人使用足夠」。

**設計原則**：
- 本機優先 —— 無伺服器元件、不監聽網路埠、資料不出機器
- 開箱即用 —— 雙擊 `start.bat` 啟動；Windows 11 預裝 Edge 即可輸出 PDF
- 降級韌性 —— 每條轉換路徑都有備援引擎，首選失敗自動降級而非直接報錯

---

## 2. 現況總覽（v2.2）

| 模組 | 狀態 | 說明 |
|------|------|------|
| 格式互轉（7 格式 25+ 方向） | ✅ 完成 | PDF / DOCX / PPTX / HTML / MD / TXT / Image |
| 掃描版 PDF OCR | ✅ 完成（2026-06-11） | 無文字層頁面自動 RapidOCR 辨識（TXT/MD/HTML 路徑），離線、零系統依賴 |
| 轉換 Profile | ✅ 完成（2026-06-11） | 首頁一鍵套用「格式+輸出目錄+覆寫」具名組合 |
| 批次轉換 | ✅ 完成 | 資料夾級批次處理 |
| PDF 工具 | ✅ 完成 | 合併 / 分割 / 壓縮 / 加密解密 / 點擊編輯文字 |
| 圖片工具 | ✅ 完成 | rembg AI 去背 / 背景替換 / 邊緣銳化羽化；模型首次使用時自動下載 |
| 歷史記錄 | ✅ 完成 | SQLite 持久化 + 一鍵重新轉換 + 每日統計 |
| 設定系統 | ✅ 完成 | 即時儲存全頁面（QSettings 持久化） |
| 系統整合 | ✅ 完成 | 托盤常駐、單實例鎖、完成通知、快捷鍵 |
| 打包分發 | ✅ 完成 | PyInstaller onedir portable exe（~600MB；AI 去背模型改為首次使用時下載） |
| 測試 | ✅ 176 passed | 轉換器層全覆蓋 + 設定/模型/OCR 單元測試（UI 層靠手動驗收） |

---

## 3. 技術架構摘要

```
UI 層        desktop/main_window_v2.py（MSFluentWindow，7 分頁）+ pages/ + widgets/
控制層       desktop/controllers/（7 個 Manager，QThreadPool 非同步）
業務層       converters/（dispatcher 路由 + 各格式 Converter + pdf_tools + image_tools）
持久層       desktop/database/history_db.py（SQLite）+ QSettings
```

技術棧：Python 3.13 · PySide6 6.11.0 · QFluentWidgets 1.11.2 · PyMuPDF · rembg

> 詳細架構、開發指令與注意事項見 `CLAUDE.md`；使用者導向說明見 `README.md`。

---

## 4. 開發歷程

| 日期 | 里程碑 |
|------|--------|
| 2026-04-24 | v1.0 / v1.1：Flask Web 版（瀏覽器 UI）+ PDF 四引擎降級鏈 |
| 2026-04-25 | v2.0：全面遷移為 PySide6 桌面應用（架構重寫，業務層保留） |
| 2026-05-22 | 個人使用驗收完成，宣告功能凍結 |
| 2026-05 下旬–06-09 | v2.1：PDF 工具頁、圖片去背、歷史重轉、托盤、單實例鎖、Nordic 雙主題 |
| 2026-06-10 | 全專案清理：40 個 Flask 遺留/死代碼檔案移除、依賴修正、文件全面同步 |
| 2026-06-11 | v2.2 迭代一輪：轉換 Profile、AI 模型延遲下載（exe 瘦身 ~1.1GB→~600MB）、掃描版 PDF OCR（RapidOCR） |

---

## 5. 維護模式準則

本專案已進入維護期，後續變更遵循以下準則：

1. **預設不擴張功能** —— 新功能僅在使用中遇到實際需求時才評估，不做「順手加」
2. **修 bug 採最小變更** —— 只動必要範圍，不順手重構鄰近代碼
3. **變更後必跑驗證** —— `pytest tests/` 全綠為底線；動到打包相關（spec / requirements / 資源）必實際執行打包驗證 exe 可啟動
4. **文件同步** —— 行為變更同步 `README.md`；架構變更同步 `CLAUDE.md`；版本變更記入 `CHANGELOG.md`
5. **編碼紅線** —— `.bat` 檔一律 UTF-8 BOM + CRLF（否則 cmd 解析中文會出錯）
6. **依賴釘版** —— UI 框架（PySide6 / QFluentWidgets）釘死版本；升級需完整回歸測試

---

## 6. 未來候選方向（Backlog）

> 以下為**記錄而非承諾**，無排程。僅在實際使用痛點出現時，按價值重新評估。

**2026-06-11 迭代一輪已完成 3 項**：掃描版 PDF OCR（採 RapidOCR，比原估的 Tesseract/PaddleOCR 更輕——複用既有 onnxruntime、模型僅 ~15MB 且隨 pip 套件離線可用）、轉換 Profile 預設組、exe 體積瘦身（AI 模型改首次使用時自動下載）。

| 候選項 | 動機 | 狀態 / 評估 |
|--------|------|------------|
| 多語言介面 | i18n 骨架已存在（`desktop/utils/i18n.py`） | **暫緩**（2026-06-11 評估）：個人使用、繁中夠用，UI 文字抽取工程量大、價值低 |
| PPTX → DOCX | 目前矩陣中唯一常見但不支援的方向 | **暫緩**（2026-06-11 評估）：v1 已評估放棄，版面語義映射複雜且實用情境存疑 |
| exe 進一步瘦身 | ~600MB 仍有壓縮空間（onnxruntime/scipy/opencv 占大宗） | 候選：值得時再評估依賴裁剪 |

---

## 7. 已知限制與技術債

**功能限制**（設計決策，不視為 bug）：
- 掃描版 PDF 的 OCR 為「盡力而為」：輸出純文字（不還原版面）；手寫、低解析度、嚴重歪斜的掃描件辨識率有限
- PPTX → DOCX 不支援；PPT 動畫不保留
- DOCX → PDF 高保真模式依賴本機 Microsoft Word（COM）；無 Word 自動降級
- 單檔 50MB / 批次 200MB 上限
- 圖片去背首次使用需網路下載模型（~175MB）；離線環境可用 `scripts/predownload_models.py` 預載

**技術債（觀察中，不主動處理）**：
- `pages/image_tools_page.py`（~76KB）與 `pages/pdf_tools_page.py`（~63KB）單檔偏大，功能正常；若未來需大改再拆分
- UI 層無自動化測試，依賴手動驗收（轉換器層已有 154 個測試守住業務邏輯）

---

## 8. 分支與發布流程

- **分支模型**：日常開發在 `develop`，穩定後 PR 合併至 `main`
- **版本規範**：語意化版本（SemVer）；每次發布同步 `CHANGELOG.md`（Keep a Changelog 格式）
- **commit 規範**：Conventional Commits（`feat:` / `fix:` / `chore:` / `docs:`）
- **發布產物**：`build.bat` 產出 `dist/文件轉檔/`（portable，免安裝），目錄整體複製即可分發
